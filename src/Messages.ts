namespace net.user1.orbiter
{
	/**
	 * Messages class defines constants representing the built-in messages defined by the Orbiter
	 * library. See [[Room.sendMessage]], [[Client.sendMessage]], and [[Server.sendMessage]].
	 */
	export enum Messages
	{
		CLIENT_HEARTBEAT = 'CLIENT_HEARTBEAT'
	}
}
