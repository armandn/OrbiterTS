namespace net.user1.orbiter.filters
{
	/**
	 * An enumeration of the types of Boolean groups in the Orbiter filtering API. Boolean groups
	 * define relationships between multiple comparisons in a filter.
	 */
	export enum BooleanGroupType
	{
		AND = 'AND',
		OR = 'OR'
	}
}
